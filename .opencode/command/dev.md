---
description: Switch to dev branch and pull latest changes
---

Switch to the `dev` branch and pull the latest changes from the remote repository.

## Workflow

1. **Check current branch** using `git branch --show-current`
2. **Switch to dev branch** using `git checkout dev`
3. **Pull latest changes** using `git pull origin dev`
4. **Report status** to the user
